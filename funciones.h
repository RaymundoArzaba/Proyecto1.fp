#ifndef FUNCIONES_H
#define FUNCIONES_H

int conv_binario_decimal(char binario[]);
char[] 
{
    for(int i = strlen(bianrio)-1; i>=0; i--){
        if(binario[i]=='1')
        num= num + pow(2, pot);
        pot = pot + 1;
    }
    return num;
}
#endif